# Content Factory — Python dependencies
# Все из MIT/Apache/BSD (коммерчески безопасно)

# --- LLM & агенты ---
anthropic>=0.50.0
openai>=1.50.0
crewai>=1.14.0
crewai-tools>=0.40.0
langgraph>=1.2.0
langchain>=0.3.20
langchain-anthropic>=0.3.0
langchain-community>=0.3.0

# --- Research ---
gpt-researcher>=3.4.4
crawl4ai>=0.5.0
duckduckgo-search>=6.0.0
beautifulsoup4>=4.13.0
trafilatura>=2.0.0
lxml>=5.4.0
playwright>=1.50.0

# --- RAG ---
qdrant-client>=1.13.0
sentence-transformers>=3.5.0
# bge-m3 загружается через sentence-transformers при первом запуске

# --- WordPress ---
requests>=2.32.0
python-wordpress-xmlrpc>=2.3       # legacy fallback
# REST API через прямые requests + JWT/Application Password

# --- Telegram ---
aiogram>=3.15.0

# --- Instagram ---
instagrapi>=2.2.1
moviepy>=2.1.0                     # обработка видео для Reels

# --- TikTok ---
tiktok-uploader>=1.2.0             # wkaisertexas/tiktok-uploader

# --- SMM unified ---
# Postiz — отдельный сервис, общаемся через REST API

# --- Images / Video ---
Pillow>=11.0.0
requests-toolbelt>=1.0.0
# ComfyUI — отдельный сервис, общаемся через API
ffmpeg-python>=0.2.0

# --- TTS (Silero для RU) ---
torch>=2.6.0
torchaudio>=2.6.0
omegaconf>=2.3.0
# Silero модели подгружаются через torch.hub при первом запуске

# --- SEO / Analytics ---
google-searchconsole>=0.0.4        # joshcarty/google-searchconsole
google-analytics-data>=0.18.0      # official GA4
gapandas4>=0.4.0                   # pandas wrapper для GA4
tapi-yandex-metrika>=2024.0        # Метрика
# yandex-webmaster — пишем через requests (нет хорошего OSS-клиента)

# --- Storage / DB ---
psycopg[binary]>=3.2.0
sqlalchemy>=2.0.36
alembic>=1.14.0
asyncpg>=0.30.0
redis>=5.2.0
minio>=7.2.0

# --- Web framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
celery>=5.4.0

# --- Templating ---
jinja2>=3.1.0
pyyaml>=6.0.0
markdown>=3.7
markdownify>=0.14.0
python-frontmatter>=1.1.0

# --- Schema.org ---
extruct>=0.18.0                    # парсер существующих schema на конкурентах

# --- Утилиты ---
loguru>=0.7.3
httpx>=0.28.0
tenacity>=9.0.0
arrow>=1.3.0
python-dotenv>=1.0.1
typer>=0.15.0
rich>=13.9.0

# --- Текст RU-обработка ---
pymorphy3>=2.0.2                   # русская морфология для LSI и internal linking
razdel>=0.5.0                      # токенизация RU

# --- Тестирование ---
pytest>=8.3.0
pytest-asyncio>=0.25.0
pytest-cov>=6.0.0
